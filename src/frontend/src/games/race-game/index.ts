/**
 * Race Game Module
 *
 * Shared race game container component for multiplication and division games.
 * Uses the bike race metaphor to build fact fluency.
 */

export { RaceGame } from './RaceGame';
export type { RaceGameProps, GameStats } from './RaceGame';
