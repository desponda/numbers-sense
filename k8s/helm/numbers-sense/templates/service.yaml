apiVersion: v1
kind: Service
metadata:
  name: {{ include "numbers-sense.fullname" . }}
  labels:
    {{- include "numbers-sense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "numbers-sense.selectorLabels" . | nindent 4 }}
